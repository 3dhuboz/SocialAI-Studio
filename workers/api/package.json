{
  "name": "@socialai/api-worker",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --config wrangler.toml --port 8787",
    "deploy": "wrangler deploy --config wrangler.toml",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@clerk/backend": "^3.2.0",
    "hono": "^4.6.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260719.1",
    "typescript": "^5.5.4",
    "vitest": "^4.1.6",
    "wrangler": "^4.112.0"
  },
  "overrides": {
    "esbuild": "^0.28.0"
  }
}
